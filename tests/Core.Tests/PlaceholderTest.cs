using Xunit;

namespace TransactionCompliance.Core.Tests;

// Placeholder — F1 contains interfaces and models only; test implementations follow in subsequent phases.
public class PlaceholderTest
{
    [Fact]
    public void F1_ModelsAndInterfaces_Exist() => Assert.True(true);
}
